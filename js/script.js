import {
  rainSoundButton,
  coffeeShopSoundButton,
  forestSoundButton,
  fireplaceSoundButton
} from './elements.js'

import Events from './events.js'

Events({
  rainSoundButton,
  fireplaceSoundButton,
  forestSoundButton,
  coffeeShopSoundButton
})
